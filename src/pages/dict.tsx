import { Typography } from "antd";
import { FC } from "react";

const Dict: FC = () => {
  return (
    <main>
      <Typography.Title level={2}>Dictionary</Typography.Title>
    </main>
  );
};

export default Dict;
